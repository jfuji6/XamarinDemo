<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
x:Class="XamarinDemo.StockListPageView">

	<ContentPage.ToolbarItems>
		<ToolbarItem 
		Text="Form"
		x:Name="FormButton"/>
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<StackLayout>
		<ActivityIndicator
			IsRunning="{Binding IsLoafding}"
			IsVisible="{Binding IsLoading}"
			VerticalOptions = "CenterAndExpand"
			HorizontalOptions= "CenterAndExpand"/>
		<ListView
		ItemsSource="{Binding StockList}"
		VerticalOptions="StartAndExpand"
		>
		<ListView.ItemTemplate>
			<DataTemplate>
				<ViewCell>
                    <StackLayout Orientation="Horizontal">
                        <StackLayout Orientation="Vertical"  >
                            <Label Text="{Binding ProductNumber}"/>
                            <Label Text="{Binding ProductDescription}"/>
                            <Label Text="{Binding LocationName}"/>

                        </StackLayout>
                        <Label WidthRequest="100" Text="{Binding Quantity}" VerticalOptions="Center" HorizontalOptions="End"/>
                    </StackLayout>
                </ViewCell>
			</DataTemplate>
		</ListView.ItemTemplate>
		</ListView>
	</StackLayout>
	</ContentPage.Content>
</ContentPage>
